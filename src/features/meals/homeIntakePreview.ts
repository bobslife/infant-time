// Development-only preview flag. Production behavior follows the current date.
// Enabling this affects presentation only; event data and summaries are never changed.
export const SHOW_EMPTY_INTAKE_PREVIEW = false;
